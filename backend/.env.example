PORT=3000
NODE_ENV=development
TEMP_DIR=/tmp/mashup-maker
MAX_ZIP_SIZE_MB=50
CLEANUP_INTERVAL_HOURS=24

# Ollama Local LLM Configuration
# Powers both the AI Chatbot and Idea Generator - NO API KEYS NEEDED!
# Make sure Ollama is installed and running: https://ollama.ai
# Install: brew install ollama (macOS) or visit https://ollama.ai/download
# Pull model: ollama pull llama3
# Start server: ollama serve
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3
